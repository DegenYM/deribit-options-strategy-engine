# 回測與黑天鵝風險報告（Deribit 公開資料）

- 產出時間：`2026-05-29T15:06:56.565658+00:00`
- 策略：`naked_short`
- 回測區間：`2024-01-01T00:00:00+00:00` → `2026-05-29T15:06:30.327297+00:00`（resolution=1D）
- 參考資金（USDC）：`1000`

## 一頁結論（先看這裡）
- **這段期間有沒有真的開倉**：`6` 筆
- **回測總損益**：`-9.61` USDC（約 `-0.96%`）
- **最大回撤（歷史回放）**：`0%`
- **黑天鵝（-40% + 滑價）最差單次估算損失**：`-333.3` USDC（約 `-33.33%`）
  - 最差發生日：`2026-05-14`；book 拆解：`{'USDC': '-333.3000'}`
  - 來源拆解（USDC）：「標的下跌→內在價增加」=`-276.21`，「滑價」=`-57.08`
- **黑天鵝（-40% + 滑價）p05（偏差情境）**：`0` USDC（約 `0%`）
- **黑天鵝（-40% + 滑價）中位數（p50）**：`0` USDC（約 `0%`）

## 怎麼解讀（超短版）
- **historical replay**：用公開資料回放策略「會不會進場/出場」與粗略 PnL（近似）。
- **stress overlay**：假設你剛好有持倉時立刻遇到暴跌，估算當下可能虧多少（偏保守，且**每本帳損失上限 = 該本帳 equity**，代表最慘被打到歸零）。
- **策略黑天鵝重點**：主要黑天鵝是標的方向行情：short put 在快速下跌時、short call 在快速上漲時都會變深度 ITM，虧損可能遠大於已收權利金。
- 你要的「黑天鵝會損失多少」主要看 **stress overlay 的 worst / p95**；你要的「收益/風險平衡」看 conservative vs baseline 的 tail loss 是否下降。

## 重要假設（請務必閱讀）
- **資料來源限制**：Deribit 公開 API 不一定提供「歷史期權價格/greeks/完整 order book」。本回測使用 index 與 DVOL 公開資料。
- **期權價格近似**：使用 **DVOL 當作波動率 proxy**，以 Black‑Scholes（\(r=0\)）估算權利金與 delta；因此結果是「策略邏輯+風控門檻」的近似回放，不是交易所撮合級回放。
- **流動性近似**：黑天鵝情境額外加上滑價（slippage）做保守估算。

## 基準回測結果（historical replay）
- **開倉筆數**：`6`
- **總損益（USDC）**：`-9.61`（約 `-0.96%`）
- **期末資金（USDC）**：`990.38`
- **最大回撤（peak-to-trough）**：`0%`

## 黑天鵝損失估算（stress overlay）
- **觀察方式**：優先看 ±30% 到 ±60% shock 的 worst / p05，以及 by_book 是否接近單本帳 equity 上限。put 看下跌、call 看上漲。
- **模型備註**：這個壓力測試會把 short option 以 shocked intrinsic 加滑價估算買回成本，並用每本帳 equity 作歸零上限。
- 指標為「**在持倉日內立刻發生 shock**」的保守估算（不含動態管理）。
- **spot_-10_slip_05%**：樣本數=865｜worst=`0`（0%）｜p05=`0`（0%）｜p50=`0`（0%）｜p95=`0`（0%）
- **spot_-20_slip_10%**：樣本數=865｜worst=`-51.35`（-5.13%）｜p05=`0`（0%）｜p50=`0`（0%）｜p95=`0`（0%）
- **spot_-30_slip_15%**：樣本數=865｜worst=`-209.95`（-20.99%）｜p05=`0`（0%）｜p50=`0`（0%）｜p95=`0`（0%）
- **spot_-40_slip_20%**：樣本數=865｜worst=`-333.3`（-33.33%）｜p05=`0`（0%）｜p50=`0`（0%）｜p95=`0`（0%）
- **spot_-50_slip_25%**：樣本數=865｜worst=`-333.3`（-33.33%）｜p05=`0`（0%）｜p50=`0`（0%）｜p95=`0`（0%）
- **spot_-60_slip_30%**：樣本數=865｜worst=`-333.3`（-33.33%）｜p05=`0`（0%）｜p50=`0`（0%）｜p95=`0`（0%）

## Risk / Profit balance 改進（參數掃描）
- 本次未執行參數掃描。

## 建議（可直接落地的調整）
- 降低 `PER_LEG_IM_CAP_PUT` / `PER_LEG_IM_CAP_CALL`、`EXPIRY_IM_CAP`、`BOOK_IM_TARGET/BOOK_IM_HARD`。
- 把 put / call 的 delta 與 OTM 範圍往保守側移，避免為了 APR 接近價外太近。
- 進入 `CRISIS` regime 時停開新倉，已有倉位優先處理最大 gamma / 最近到期腿。
