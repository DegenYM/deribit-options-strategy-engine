# 回測與黑天鵝風險報告（Deribit 公開資料）

- 產出時間：`2026-04-28T00:46:44.316910+00:00`
- 回測區間：`2026-01-01T00:00:00+00:00` → `2026-04-28T00:46:35.248101+00:00`（resolution=1D）
- 參考資金（USDC）：`6300`

## 一頁結論（先看這裡）
- **這段期間有沒有真的開倉**：`20` 筆
- **回測總損益**：`134.66` USDC（約 `2.13%`）
- **最大回撤（歷史回放）**：`0%`
- **黑天鵝（-40% + 滑價）最差單次估算損失**：`-3636.19` USDC（約 `-57.71%`）
  - 最差發生日：`2026-04-28`；book 拆解：`{'USDC': '-1479.4597004672422938000', 'ETH': '-2156.732213710947292818600132'}`
  - 來源拆解（USDC）：「標的下跌→內在價增加」=`-2999.25`，「滑價」=`-636.93`
- **黑天鵝（-40% + 滑價）p05（偏差情境）**：`-2094.63` USDC（約 `-33.24%`）
- **黑天鵝（-40% + 滑價）中位數（p50）**：`0` USDC（約 `0%`）

## 怎麼解讀（超短版）
- **historical replay**：用公開資料回放策略「會不會進場/出場」與粗略 PnL（近似）。
- **stress overlay**：假設你剛好有持倉時立刻遇到暴跌，估算當下可能虧多少（偏保守，且**每本帳損失上限 = 該本帳 equity**，代表最慘被打到歸零）。
- 你要的「黑天鵝會損失多少」主要看 **stress overlay 的 worst / p95**；你要的「收益/風險平衡」看 conservative vs baseline 的 tail loss 是否下降。

## 重要假設（請務必閱讀）
- **資料來源限制**：Deribit 公開 API 不一定提供「歷史期權價格/greeks/完整 order book」。本回測使用 index 與 DVOL 公開資料。
- **期權價格近似**：使用 **DVOL 當作波動率 proxy**，以 Black‑Scholes（\(r=0\)）估算權利金與 delta；因此結果是「策略邏輯+風控門檻」的近似回放，不是交易所撮合級回放。
- **流動性近似**：黑天鵝情境額外加上滑價（slippage）做保守估算。

## 基準回測結果（historical replay）
- **開倉筆數**：`20`
- **總損益（USDC）**：`134.66`（約 `2.13%`）
- **期末資金（USDC）**：`6434.66`
- **最大回撤（peak-to-trough）**：`0%`

## 黑天鵝損失估算（stress overlay）
- 指標為「**在持倉日內立刻發生 shock**」的保守估算（不含動態管理）。
- **spot_-10_slip_05%**：樣本數=118｜worst=`0`（0%）｜p05=`0`（0%）｜p50=`0`（0%）｜p95=`177.64`（2.81%）
- **spot_-20_slip_10%**：樣本數=118｜worst=`-780.96`（-12.39%）｜p05=`-491.33`（-7.79%）｜p50=`0`（0%）｜p95=`34.29`（0.54%）
- **spot_-30_slip_15%**：樣本數=118｜worst=`-2437.99`（-38.69%）｜p05=`-1708.37`（-27.11%）｜p50=`0`（0%）｜p95=`0`（0%）
- **spot_-40_slip_20%**：樣本數=118｜worst=`-3636.19`（-57.71%）｜p05=`-2094.63`（-33.24%）｜p50=`0`（0%）｜p95=`0`（0%）
- **spot_-50_slip_25%**：樣本數=118｜worst=`-4313.46`（-68.46%）｜p05=`-2319.77`（-36.82%）｜p50=`0`（0%）｜p95=`0`（0%）
- **spot_-60_slip_30%**：樣本數=118｜worst=`-4313.46`（-68.46%）｜p05=`-2416.1`（-38.35%）｜p50=`0`（0%）｜p95=`0`（0%）

## Risk / Profit balance 改進（參數掃描）
- 下列比較以同一套回測 + stress overlay 產出；挑選方向是「**降低尾端損失**，在可接受的收益犧牲下」：
- **baseline**：pnl=134.661121 mdd=0 stress_p05(-40%)=-2094.636682 stress_worst(-40%)=-3636.191914 worst_date=2026-04-28 (BOOK_IM_TARGET=0.35, BOOK_IM_HARD=0.45, PER_LEG_IM_CAP_PUT=0.15, EXPIRY_IM_CAP=0.35, MIN_NET_APR=0.12)
- **conservative**：pnl=53.488992 mdd=0 stress_p05(-40%)=-1061.839164 stress_worst(-40%)=-1874.951674 worst_date=2026-04-28 (BOOK_IM_TARGET=0.30, BOOK_IM_HARD=0.40, PER_LEG_IM_CAP_PUT=0.12, EXPIRY_IM_CAP=0.25, MIN_NET_APR=0.14)
- **profit_seek**：pnl=132.2448 mdd=0 stress_p05(-40%)=-2094.636682 stress_worst(-40%)=-3972.364664 worst_date=2026-04-28 (BOOK_IM_TARGET=0.38, BOOK_IM_HARD=0.50, PER_LEG_IM_CAP_PUT=0.18, EXPIRY_IM_CAP=0.35, MIN_NET_APR=0.10)

## 建議（可直接落地的調整）
- **優先降槓桿（尾端風險最大來源）**：把 `BOOK_IM_TARGET/BOOK_IM_HARD` 往下調，並同步收緊 `PER_LEG_IM_CAP_PUT` 與 `EXPIRY_IM_CAP`。
- **把收益門檻改成「先活著」**：適度提高 `MIN_NET_APR`，但搭配更低 IM cap，避免為了 APR 追逐更危險的近價 put。
- **黑天鵝時段處置**：在 `CRISIS` regime 下（index 大跌或 DVOL 飆升）停開新倉；若已開倉，優先把最差 expiry/最大 gamma 的部位降槓桿。
