# Optional PDF rendering deps — mirrors [project.optional-dependencies].pdf in pyproject.toml.
# Needed only for investor fee-report PDFs and scripts/generate_investor_*_pdf.py.
reportlab>=4.0,<6
matplotlib>=3.8,<4
numpy>=1.26,<3
pillow>=12.3,<13
